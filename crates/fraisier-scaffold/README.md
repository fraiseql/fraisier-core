# fraisier-scaffold

Generators for the files fraisier writes to disk:

- [`starter_config`] — the commented starter `fraisier.toml` that `fraisier init`
  drops into a fresh project. It validates clean against `fraisier-config`.

The `scaffold` systemd/socket/nginx/CI templates live here too.
